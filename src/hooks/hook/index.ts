import { UseHookType } from './hook.type';

/**
 * A hook that manages a state variable (number) that can be incremented or decremented.
 *
 * @returns An object with:
 *  - name
 */
export const useHook: UseHookType = () => ({
  name: '',
});
