import { css } from 'configs';

export const selectedMentionOption = css({});

export const mentionOption = css({});

export const nodeStyle = css({
  fontWeight: '$600',
  color: '$secondary400',
});
